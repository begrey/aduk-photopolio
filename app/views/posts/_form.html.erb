<%= form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  

  <div class="field">
    <%= f.label :제목 %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :내용 %><br>
    <%= f.text_area :content, cols: "50", rows: "10" %>
  </div>
  <div class="field">
    <%= f.label :영상url첨부 %><br>
    <%= f.text_area :videourl, cols: "100", rows: "2" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
